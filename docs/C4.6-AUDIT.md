# C4.6 audit — 2026-09-07

## Qaytarilish sababi

Screenshotdagi 2026-09-06 izohi va
`docs/trello-testing-audit.mjs` dagi C4.6 yozuvi bir xil sababni ko‘rsatadi:
**TC1 va TC3 bajarilgani tasdiqlanmagan** — productionda register → payout
uzluksiz oqimi o‘tkazilmagan, servis yiqilganda alert kelishi sinalmagan.
`docs/trello-notify.mjs` ham shu ikki bandni qolgan ish deb belgilaydi.
Card checklistida TC1–TC4 bor; screenshotda 0/4 belgilangan.

Bu faqat unit test yetishmasligi emas: productionda ishlagan biznes oqimi,
HTTPS, alert yetib borishi va restore dalillari talab qilinadi.

## Production dalillari

Server: foydalanuvchi ko‘rsatgan `169.58.98.223`, `/srv/marketplace`.
SSH orqali read-only konfiguratsiya, container runtime va umumiy SQL countlar
tekshirildi. Maxfiy qiymatlar chiqarilmadi. Live bazaga yozilmadi, servislar
ataylab to‘xtatilmadi, yangi ilova kodi deploy qilinmadi.

| TC | Tekshirilgan holat | Xulosa |
| --- | --- | --- |
| TC1 register → payout | `payment.provider_config`: 0; `payment.payment`: 0; `finance.payout`: 0; online DELIVERED seller order: 0 | O‘tmagan. Provayder sozlamasi va ajratilgan test oqimi kerak. |
| TC2 production HTTPS/TLS | `.env.production` **va ishlayotgan Caddy**: `DOMAIN=:80`, `APP_DOMAIN=kabinet.localhost` | Origin HTTP rejimida. Public HTTPS domeni taqdim qilinmagan, TC2 tasdiqlanmadi. Screenshotdagi oldingi izoh yetarli dalil emas. |
| TC3 servis yiqilganda alert + log | Monitor readiness URLni tekshiryapti, loglarda `healthy`; **runtime** `webhookConfigured=false` | Real alert yuborish sozlanmagan. Firing + resolved qabul qilinish sinovi qolgan. |
| TC4 DB backup + restore | `elchi-marketplace-20260907T094513Z.dump` SHA-256 OK; alohida PostgreSQL 16 konteynerida strict restore va haqiqiy jadvallar tekshiruvi OK | Shu backup uchun o‘tdi. Live DB o‘zgarmadi, vaqtinchalik konteyner olib tashlandi. |

HTTP `/api/v1/health/readiness` 200: barcha 11 mikroservis `ready=true`.
Gateway, PostgreSQL, RabbitMQ va MinIO healthy; boshqa servislar Up.
Bu liveness/readiness dalili, to‘lov yoki alert ishlaganining dalili emas.
Tashqi, bu serverdan tashqaridagi TLS proxy mavjudligi aniqlanmagan;
uning domeni berilsa public HTTPS alohida tekshirilishi kerak.

## Tuzatilgan kod

- Oddiy smoke login tokenini amaldagi raw `accessToken` javobidan oladi.
- `deploy/e2e-payout.json`: register, cart, online checkout, payment,
  tasdiqlangan order/shipment, aynan shu seller orderga tegishli payout va
  yakuniy PAID/paidAt tekshiruvi. Provayder to‘lovi, Elchi yetkazilishi va
  SUPERADMIN approve/release amallari operator tomonidan bajariladi.
- Runner: numeric capture turi saqlanadi; faqat GET polling; unrelated payout
  testni o‘tkazmaydi; uzoq kutishda admin qayta login; passed/failed report
  va biznes IDlari. Response body va tokenlar reportga yozilmaydi.
- Monitor: ishlamagan firing/resolved webhook keyingi tickda qayta urinadi;
  recovery webhook xatosi health failure hisoblagichini oshirmaydi;
  tekshiruvlar ustma-ust ishlamaydi. `alert_unconfigured` va muvaffaqiyatli
  yuborilishdan keyingi `alert_delivered` loglari qo‘shildi. HTTP xato logida
  webhookning maxfiy path/query qismi chiqarilmaydi.
- Backup: yopiq fayl ruxsatlari, unique nom va `.partial` orqali nashr;
  xato dump tayyor backup sifatida qolmaydi.
- Restore: cheklangan readiness kutishi, `--exit-on-error`, haqiqiy
  identity/catalog/checkout/payment/finance jadvallari, xatoda cleanup.
  Eski schema count bo‘sh bazadagi `public` sabab noto‘g‘ri ijobiy natija
  berishi mumkin edi.
- `ops:c46-preflight` production uchun yetishmayotgan domen, alert va Elchi
  webhook sozlamalarini nomlari bilan bildiradi. Bu konfiguratsiya tekshiruvi
  TC natijasining o‘rnini bosmaydi.
- Ops regressiya testlari va CI ichida `npm run test:ops` qo‘shildi.
  Runbookda production qadamlar va dalil talablari yangilandi.

## Tekshiruvlar

| Tekshiruv | Natija |
| --- | --- |
| `npm run lint` | PASS |
| `npm run format:check` | PASS |
| `npm run build:all` | PASS — 12 app |
| `npm test -- --runInBand` | 92 suite, 343 test PASS; DBsiz bosqichda 4 integration test skip |
| PostgreSQL integration | Alohida lokal PostgreSQL 16 test bazasida qolgan 4 test PASS |
| Inventory coverage | 42 test PASS; statements 89.17%, branches 58.82%, functions 95.23%, lines 89.26%; CI thresholdlari PASS |
| `npm run test:ops` | 4 test fayli PASS: runner, preflight, monitor, backup/restore |
| OpenAPI export + diff | PASS — 102 path, `docs/openapi.json` o‘zgarmadi |
| Caddy Docker validate | PASS — CI bilan bir xil image va konfiguratsiya |
| Shell syntax / `git diff --check` | PASS |
| CI workflow YAML va production Compose | PASS — example env vaqtinchalik katalogda ishlatildi |
| Production backup restore | PASS — vaqtinchalik PostgreSQL 16, strict restore va core tables |

CI validatsiya amallari lokal bajarildi va ops testlar workflowga qo‘shildi.
GitHub Actions remote run/deploy ishga tushirilmadi: commit va push qilinmadi.
Mock E2E test real payment/payout bajarilganini anglatmaydi.

## Done qarori

**Hozir Done’ga o‘tkazish mumkin emas.** TC4 tasdiqlandi, TC1–TC3 qolgan.
Yakunlash uchun:

1. Haqiqiy public API domeni/DNS va HTTPSni tasdiqlash (yoki mavjud tashqi
   TLS proxy domenini ko‘rsatish).
2. Alert adapter manzilini sozlash, yangi monitor kodini deploy qilish,
   servis nosozligi va tiklanishida firing/resolved kanalga kelishini qayd etish.
3. To‘lov provayderi va Elchi sozlamalari, ajratilgan test product/variant,
   admin va yangi test buyer bilan `e2e-payout.json` ni oxirigacha bajarish;
   `passed` report va order/payment/shipment/payout IDlarini saqlash.

Domen va webhook manzili foydalanuvchidan so‘raldi; ularning o‘rniga
qiymat taxmin qilinmadi. Production akkaunt/to‘lov/shipment yozuvlari
yaratilmadi. Mavjud C4.5 tuzatishi va boshqa user kodi o‘zgartirilmadi.
